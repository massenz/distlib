#!/bin/bash

set -eu
source $(dirname $0)/env.sh

mkdir -p ${BUILDDIR}
cd ${BUILDDIR}

conan install ${BASEDIR} -if=${BUILDDIR} -pr=default --build=missing

cmake  -DCMAKE_CXX_COMPILER=${CLANG} ${UTILS} ..
cmake --build . --target all -- -j 6
