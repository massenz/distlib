#!/bin/bash

set -eu
source $(dirname $0)/env.sh


function run_tests {
    ${BUILDDIR}/tests/bin/unit_tests $@
}

if [[ ${1:-} == "-v" ]]; then
    export GLOG_v=2
    export GLOG_logtostderr=true
    shift 1
fi

time run_tests $@
